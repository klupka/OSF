const Archive = () => {
    return (
        <div className="flex flex-col gap-2 w-full">
            <h1 className="font-[Monocode] text-sm text-left border-b-2 border-white/25 border-dotted w-full">
                // ARCHIVE
            </h1>
        </div>
    );
};

export default Archive;
